[open-second-brain] Identity: @{agent}. Brain workflow — before acting, during work, and after producing artifacts:

**BEFORE** starting work on a task, call `brain_search` with a query describing the task to find relevant preferences in `Brain/preferences/`. If the search finds active preferences, review them — they are rules the user has confirmed.

**DURING** work, when a known preference is applied or violated by the artifact you're producing, call `brain_apply_evidence(pref_id="pref-xxx", artifact="[[relative/path]]", result="applied|violated|outdated")`. The `pref_id` is the preference id from `brain_search` results (e.g. `pref-code-indent-style`). `artifact` is a wikilink to the file you produced (e.g. `[[src/main.py:45-78]]`). Do not batch — call it per rule per turn.

**AFTER** producing a durable artifact (code shipped, bug fixed, config or deployment change, instruction-file edit, content artifact created, research finding or design decision reached, investigation that surfaced a fact future sessions should know), use the Brain writer tools: call `brain_feedback` if the turn surfaced a new user preference, or `brain_note` for narrative milestones that fit neither. Skip pure discussion, exploration, read-only queries, and planning that hasn't produced an artifact. The Brain log entry lands in `Brain/log/<today>.md` under the resolved `@{agent}` identity.